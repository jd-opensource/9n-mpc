apiVersion: v1
kind: Service
metadata:
  name: svc-dc-{{ task_id }}
  namespace: {{namespace}}
  labels:
    role: datacenter
    instanceId: "{{ task_id }}"
spec:
  selector:
    role: datacenter
    instanceId: "{{ task_id }}"
  ports:
    - protocol: TCP
      port: 50052
      targetPort: 50052
